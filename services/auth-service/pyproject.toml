[project]
name = "aisc-auth-service"
version = "0.1.0"
description = "AISC Authentication Service"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src*"]
